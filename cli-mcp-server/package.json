{
  "name": "cubeapm-cli-mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CLI-backed MCP server bridge exposing the cubeapm CLI (traces/logs/metrics) to MCP clients such as Claude Desktop",
  "main": "index.js",
  "scripts": {
    "check": "node --check index.js",
    "test": "node --check index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  }
}
