version: 2

project_name: cubeapm-cli

builds:
  - binary: cubeapm
    main: .
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X github.com/piyush-gambhir/cubeapm-cli/cli-go/cmd.Version={{.Version}}
      - -X github.com/piyush-gambhir/cubeapm-cli/cli-go/cmd.Commit={{.Commit}}
      - -X github.com/piyush-gambhir/cubeapm-cli/cli-go/cmd.BuildDate={{.Date}}

sboms:
  - artifacts: archive
    disable: false

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
